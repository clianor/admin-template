import { Module } from '@nestjs/common';import { TypeOrmModule } from '@nestjs/typeorm';import { AuthGroups } from '@server/entities/auth-groups.entity';import { Users } from '@server/entities/users.entity';import { SeedsService } from '@server/modules/seeds/seeds.service';@Module({  imports: [TypeOrmModule.forFeature([AuthGroups, Users])],  providers: [SeedsService],})export class SeedsModules {  constructor(private readonly seedService: SeedsService) {    this.initSeedData().then(() => console.log('init seed data'));  }  async initSeedData() {    if (process.env.NODE_ENV !== 'production') {      await this.seedService.createGroup();      await this.seedService.createAdmin();    }  }}