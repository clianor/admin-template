import { NextPage } from 'next';const Home: NextPage<{ email: string }> = (props) => {  const { email } = props;  return (    <h1 className="mt-12 text-3xl font-bold text-center">Hello {email}</h1>  );};Home.getInitialProps = ({ query }) => {  return {    email: query.email as string,  };};export default Home;