import { NextPage } from 'next';export type HomeProps = {  email: string;};const Home: NextPage<HomeProps> = (props) => {  const { email } = props;  return (    <h1 className="mt-12 text-3xl font-bold text-center">Hello {email}</h1>  );};Home.getInitialProps = ({ query }) => {  return query as HomeProps;};export default Home;